{
  "name": "Youth Station Dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.10",
  "forwardPorts": [5000],
  "portsAttributes": {
    "5000": {
      "label": "Youth Station Web",
      "onAutoForward": "openPreview"
    }
  },
  "postCreateCommand": "cd web && pip install -r requirements.txt && [ ! -f .env ] && cp .env.example .env || true",
  "postAttachCommand": "echo '✅ 环境就绪。请编辑 web/.env 后运行: cd web && python app.py'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  }
}
